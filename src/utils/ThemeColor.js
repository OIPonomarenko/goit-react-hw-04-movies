import { createTheme } from '@mui/material/styles';

export let theme = createTheme({
  palette: {
    primary: {
      main: '#9ca4ad',
      light: '#3e3e40', 
      dark: '#090a0e',
      contrastText: '#9d2658',

    //   secondary: '#132d4e',
      
    }
  },
});

